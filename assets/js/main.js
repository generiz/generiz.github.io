
$(document).ready(function () {
    $(".js-tilt").tilt({
      maxTilt: 20,
      perspective: 1000,
      easing: "cubic-bezier(.03,.98,.52,.99)",
      scale: 1.05,
      speed: 800,
      transition: true,
    });
  });
  